{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>

            <tr>
                <th>Articles with tag:</th>

                {% for article in tag.articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.category.name }}</td>
                <td>{{ article.title }}</td>
            </tr>
            {% else %}
                <tr>
                    <td colspan="4">Pas d'articles associés</td>
                </tr>
            {% endfor %}


            </tr>

        </tbody>
    </table>

    <a href="{{ path('tag_index') }}">back to list</a>

    <a href="{{ path('tag_edit', {'name': tag.name}) }}">edit</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
